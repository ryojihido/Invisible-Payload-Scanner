# セキュリティポリシー

## 想定している使い方

Invisible Payload Scannerは、ローカルで動く一次スクリーニングツールです。不可視Unicode列、既知のサプライチェーンIoC文字列、パッケージ名とバージョンの候補、自動実行設定を確認するためのものです。

このツールは完全なマルウェアスキャナではありません。検出がない場合でも、そのプロジェクトが安全であることを保証するものではありません。

## セキュリティ上の問題を報告する場合

このスキャナ自体の脆弱性を見つけた場合は、リポジトリがPrivate Security Advisoryに対応していればそちらを使うか、リポジトリで案内されている連絡先へ報告してください。

報告に含めてほしい情報:

- 影響するバージョンまたはコミット
- OSとPowerShellのバージョン
- 再現手順
- 期待した動作
- 実際の動作
- ローカルファイル内容、パス、スキャン結果が外部へ漏れる可能性があるか

公開issueには、非公開のソースコードや機密性のあるスキャン結果を貼らないでください。

## 対象範囲

対象範囲に含むもの:

- ローカルHTTPサーバのバインドやリクエスト処理
- 意図しない外部ネットワーク送信
- ユーザーが指定したスキャンパス外への不安全なファイルアクセス
- 表示されたスキャン結果によるクロスサイトスクリプティング
- 通常利用や壊れたファイルによるクラッシュ

対象範囲に含まないもの:

- 設定された不可視Unicodeパターンや同梱IoCルールに一致しないマルウェアの見逃し
- ファイルは存在するが、選択されたフィルタに一致しなかった悪性ファイル
- C2通信やcredential harvesting挙動の網羅的な検出
- npm、pnpm、yarn、bun、PyPIなどのパッケージレジストリ全体の信頼性検証
- インストール時やビルド時に外部から取得されるコード
- バイナリ型、メモリ上だけで動くもの、高度に変形された亜種
- このスキャナ外で発生したOS侵害
- スキャン対象プロジェクトそのものの安全性

## 安全設計

- ローカルサーバは `127.0.0.1` にだけバインドします。
- 起動ごとにランダムなローカルAPIトークンを生成し、スキャンと停止APIに要求します。
- ブラウザが `Origin` ヘッダーを送る場合は、許可されたローカルOriginだけを受け付けます。
- ファイルは読み取るだけで、実行しません。
- `npm install`、`pnpm install`、`yarn install`、`bun install` などのパッケージマネージャコマンドは実行しません。
- 同梱IoCルールはローカルファイルから読み込みます。自動で外部へルール更新を取りに行きません。
- 結果の抜粋に含まれるトークンらしい文字列はマスクします。
- スキャン結果はHTMLとしてではなく、テキストとしてUIへ挿入します。
- ジャンクションやシンボリックリンクなどの再解析ポイントは追跡しません。
- 正規表現マッチングにはファイルごとのタイムアウトを設定しています。
- 既定のしきい値は、絵文字由来の一般的な誤検知を減らすよう調整しています。
- リクエスト本文サイズ、カスタム正規表現長、最大ファイルサイズ、候補ファイル数には上限があります。

## Supply Chain IOC Scanの限界

Supply Chain IOC Scanは、実行前の静的チェックです。既知IoC文字列、同梱ルールに含まれる影響バージョン候補、VS Codeのfolder-open task、Claude Code / AIエージェントhooks、install-time lifecycle scriptsを確認できます。

この機能は、プロジェクトが安全であることを証明しません。パッケージ名だけの検出は、確認のきっかけであって、侵害の証拠ではありません。

`Critical` や `High` が出た場合は、そのプロジェクトをインストール・ビルド・実行する前に止めてください。入手元を確認し、公式アドバイザリとパッケージバージョンを比較してください。すでに実行済みで、認証情報にアクセスできる環境だった場合は、GitHub token、npm token、APIキー、SSH鍵などのローテーションを検討してください。

lockfileの検出は慎重に扱います。初期実装では、構造的に確定できないlockfile内のパッケージ名とバージョンの近傍一致は「確認候補」として扱います。`package.json` から既知の影響パッケージ名とバージョンが直接確認できる場合だけ、より強い警告にします。
